# Install PyTorch CPU-only first (avoids downloading ~2GB CUDA packages):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# Then install the rest:
#   pip install -r requirements.txt

torch
whisperx
pyannote.audio>=3.1
python-dotenv
tqdm

# Development / testing
pytest
